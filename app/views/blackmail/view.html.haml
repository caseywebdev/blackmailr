%h1 Blackmail View

.blackmail
  .header
    .name= @blackmail.victim_name
    .time
      Expires in
      - @time_left = DateTime.now - @blackmail.expired_at.to_datetime
      - if(@time_left.to_i <= 0)
        %span
        ="EXPIRED"
      - else
        %span.countdown{data: {'countdown-seconds' => @time_left.to_i}}
    .title= @blackmail.title
  .main
    - 5.times do|k|
      - if(FileTest.exists?("#{Rails.root}/app/assets/images/blackmail/#{@blackmail.id}_#{k}.jpg"))
        - @location = "/assets/blackmail/#{@blackmail.id}_#{k}.jpg"
        = image_tag @location
        %br
    .description= @blackmail.description
    %br
    = "demands:"
    %br
    - @b_demands.each do |d|
      = d[:description].to_s
      %br
  .footer



