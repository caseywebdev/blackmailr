%h1 Home 

%table
  %thead
    %tr
      %th(colspan = 5)
        Blackmail List
    %tr
      %th
        Title
      %th
        Expires
      %th
        Victim
      %th
        Desc.
      %th
        Countdown
  - @blackmail.each do |black|
    %tbody
      %tr
        %td
          =link_to black.title, { :controller => "blackmail", :action => "edit", :id => black.id }
        %td
          =black.expired_at
        %td
          =black.victim_name
        %td
          =black.description
        %td
          -#Not outputting right result
          %span.countdown{data: {'countdown-seconds' => (black.expired_at - Time.now)}}